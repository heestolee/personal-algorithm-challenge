/**
 *Implement a function which convert the given boolean value into its string representation.

Note: Only valid inputs will be given.
 */

export default function booleanToString(b){
  return String(b);
}
